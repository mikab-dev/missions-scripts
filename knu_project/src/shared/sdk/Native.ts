import { Npc } from "./Npc";
import { Vehicle } from "./Vehicle";

export class Native {
  static TaskVehicleEscort(ped: Npc, vehicle: Vehicle, targetVehicle: Vehicle, mode: number, speed: number, drivingStyle: number, minDistance: number, p7: number, noRoadsDistance: number): void {
    // global.TaskVehicleEscort()
  }
}
